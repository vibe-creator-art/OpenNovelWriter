# Runtime backups

This directory stores local backups created by maintenance and migration scripts.
Backup contents are ignored by Git because they may contain database records,
credentials, or other private application data.
